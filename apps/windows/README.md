# SyncDows for Windows

This directory is reserved for the Windows peer. SyncDows will use the shared mesh protocol, synchronization core, and desktop UI while providing Windows-specific storage, Explorer, system-tray, notification, network, and installer adapters.

Android and SyncTosh now consume the extracted shared modules and pass the cross-platform compatibility fixtures. Windows implementation can begin when that platform phase is approved.
